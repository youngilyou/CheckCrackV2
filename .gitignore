# === Python ===
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# === C# / .NET build output ===
viewer/**/bin/
viewer/**/obj/
viewer/**/*_wpftmp.csproj
viewer/.vs/
*.vsidx
*.suo

# === Native CMake build output + vendored FastDDS SDK (installed locally via
#     tools/Get-FastDdsGenModule.ps1, not committed -- same convention as
#     FacadePreviewer/AnalysisLoadBalancer's own .gitignore) ===
**/build/
tools/ExtraModule/
tools/Module/

# === Large generated / captured data (keep folder structure only) ===
datasets/*
!datasets/README.md
facades/*
!facades/README.md
reports/*
!reports/README.md
/facade_hierarchy.json

# === Model weights / training runs / logs / caches (regenerable, not source) ===
runs/
/models/
training/crack_seg/dataset/
training/crack_seg/dataset_labeled/
training/crack_seg/dataset_raw_crops/
training/crack_seg/dataset_stitched/
training/export/
/training_data_raw_crops/
/training_data_stitched/
logs/
metadata/*.parquet
metadata/*.json
*.pt

# === Vendored illumination-correction model (PhaSR + Depth-Anything-V2 checkpoints,
# ~2.2GB, CC-BY-NC licensed weights -- not committed while this stays non-commercial) ===
src/illumination/phasr_vendor/

# === Local scratch ===
scratch_label_example.png
tools/crack_annotator/output/

# === CheckCrackViewer 원격 분석 연동: SFTP로 받은 zip/압축해제 결과 + 처리한 archive_id
#     기록 (재시작 시 재처리 방지용, DDS durability와 무관하게 로컬 파일로 보존) -- 전부
#     런타임에 생성되는 데이터, 소스 아님. ===
remote_downloads/

# === Claude Code local session config (not meant to be shared) ===
.claude/
